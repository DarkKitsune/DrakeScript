function Vector(x, y, z)
{
	let function addFunc(a, b)
	{
		return Vector(a.x + b.x, a.y + b.y, a.z + b.z);
	}

	return {
		"x": x,
		"y": y,
		"z": z,
		"Add": addFunc
	};
}

PrintLn(Vector(1, 2, 3) + Vector(2, 3, 4));